openapi: 3.0.3
info:
  title: MergeFlow Test Repo API
  version: 1.0.0
  description: This OpenAPI specification reflects the current state of the MergeFlow Test Repo API after PR #43, which reverted PR #42. The `/health` endpoint has been removed.
paths: {}
