openapi: 3.0.3
info:
  title: MergeFlow Test Repo API
  version: 1.0.0
  description: This OpenAPI document reflects the state of the API after PR 45, which reverted PR #44. The `/health` endpoint has been removed, and no new endpoints were introduced.
paths: {}
